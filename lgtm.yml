extraction:
    cpp:
        before_index:
            - sudo apt-get install autoconf2.13
        index:
            build_command:
                - ./build.sh
    javascript:
        index:
            filters:
                - exclude: "**/*"
    python:
        index:
            filters:
                - exclude: "**/*"
